# pypi-release-action-fixture

Throwaway fixture package whose build task always fails, used to test that
build-package fails closed when the build command itself errors out.
Not published for real use.
