# pypi-release-action-fixture

Throwaway fixture package with a non-default build task name, used to test
that build-package honors the `build-command` input instead of assuming
`inv build-package`. Not published for real use.
